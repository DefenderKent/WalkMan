import {StyleSheet} from 'react-native';
import {Colors} from '../../style';

export const styles = StyleSheet.create({
  container: {
    paddingVertical: 5,
    flexDirection: 'row',
    alignItems: 'center',
  },
  title: {
    color: Colors.aqua,
    marginLeft: 10,
  },
});
